name: flutter_esp_control
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0  # 加這個
  
  cupertino_icons: ^1.0.8
  http: ^1.1.0        # ★★★ 加這行（正式引入 http 套件）

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
